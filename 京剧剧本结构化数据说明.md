# 京剧剧本结构化数据说明文档

> **脚本**：`mineru_batch_convert_structured_llm_final_v8.py`  
> **解析器版本**：`2026-05-16-r6`（代码已升级；磁盘数据可能仍为 r4，见下文「当前数据状态」）  
> **输入**：`opera_dataset/`（PDF）  
> **输出**：`opera_output/<合集>/<剧目>/`  
> **全库汇总**：`opera_output/all_*.csv`、`opera_output/all_structured.jsonl`

---

## 当前数据状态（截至本地批处理结果）

| 指标 | 数值 | 说明 |
|------|------|------|
| 已处理剧目 | 50 | `opera_output/all_docs.csv` |
| 解析器版本 | 均为 **r4** | 扁平目录，尚未重跑 r6 |
| 质量 high / medium / low | 35 / 1 / 14 | 低质量多为 PDF 表格压成 narration |
| `analysis_ready` | **36** | 质量分≥0.45 且场次≥2 且台词≥15 |
| LLM 已开启 | 50 | 文档/角色/场次级增强（r4） |
| r6 新能力 | 未落盘 | 台词 LLM、关系 LLM 校正、分层目录需重跑或 repack |

**结论**：数据**已可直接用于赛题分析与可视化**，但建议：  
1）全库统计仅用 `analysis_ready=True` 的 36 部；  
2）升级至 r6 后获得分层目录与更强 LLM 字段（见第九节命令）。

---

## 一、赛题目标与数据设计总览

赛题从 **行当、人物关系、主题、叙事结构、综合可视** 五方面分析京剧剧本。  
本流水线输出 **可关联、可建图、可回溯证据** 的多层表，而非仅 Markdown。

### 1.1 概念映射（1 文档 + 5 核心表 + 图谱层）

| 概念 | 数据表 |
|------|--------|
| 文档总表 | `01_meta/documents.csv` + `all_docs.csv` |
| 角色表 | `02_cast/roles.csv` |
| 场次表 | `03_script/scenes.csv` |
| 台词表 | `03_script/dialogues.csv` |
| 唱念做打 | `03_script/performances.csv` |
| 关系表 | `04_graph/relations.csv` + `relations_aggregated.csv` |
| 主题表 | `05_themes/themes.csv` + `themes_aggregated.csv` + `theme_pairs.csv` |
| 图谱/衍生 | `04_graph/network_metrics.csv`、`entity_aliases.csv`；`06_narrative/narrative_curve.csv` |
| 一站式 JSON | 剧目根目录 `structured.json` |

### 1.2 单剧目录结构（r6 推荐）

```
opera_output/01000000/01001001_空城计/
├── README.txt                 # 目录说明
├── structured.json            # 全量 JSON（前端/分析主入口）
├── 01_meta/documents.csv      # 文档元数据（1 行/剧）
├── 02_cast/roles.csv          # 角色画像
├── 03_script/
│   ├── scenes.csv             # 场次叙事
│   ├── dialogues.csv          # 台词原子
│   └── performances.csv       # 唱念做打
├── 04_graph/
│   ├── relations.csv          # 分场关系（证据溯源）
│   ├── relations_aggregated.csv  # 全剧网络（任务二主表）
│   ├── network_metrics.csv    # 中心性
│   └── entity_aliases.csv     # 别名归一
├── 05_themes/
│   ├── themes.csv
│   ├── themes_aggregated.csv
│   └── theme_pairs.csv
├── 06_narrative/narrative_curve.csv
└── audit/
    ├── cleaned_full.md        # 清洗后全文
    └── structured_raw.json    # 解析快照
```

### 1.3 旧版扁平目录（r4，当前 50 部）

若未见 `01_meta/` 等子目录，则 CSV 均在剧目根目录（`documents.csv`、`roles.csv`…）。  
可用 **`--repack-only`** 重组为 r6 分层（不重新调用 LLM，字段不变）。

---

## 二、数据处理流水线

```
PDF → MinerU → Markdown 清洗
    → 规则解析（场次/角色/台词/关系启发式）
    → LLM 增强（可选 --llm-enabled）
    → finalize（ID、别名、质量分、衍生表、台词/关系 LLM 二次增强 r6）
    → 分层写出 + 全库 all_*.csv
```

### 阶段 1：文档获取与清洗

- MinerU OCR → `full.md`
- 去页眉「中国京剧戏考」、页码、TCPDF、网址等
- 审计文本：`audit/cleaned_full.md`（及可选 `raw_full.md`）

### 阶段 2：规则解析（始终运行）

| 对象 | 方法 |
|------|------|
| 前言元数据 | `extract_preface_metadata` |
| 角色表 | `parse_role_section` |
| 场次 | `【第×场】` |
| 台词 | 说话人 + 板式；HTML 表格拆行 |
| 伪说话人 | 「启禀丞相」→ 报子/童儿等 |
| 启发式关系 | 邻接对话、提及、同场共现 |
| 启发式主题 | `THEME_KEYWORDS` |

### 阶段 3：大语言模型增强（`--llm-enabled`）

| 环节 | r6 行为 | 落盘字段 |
|------|---------|----------|
| **文档元数据** | 前言 + 集合信息 + 约 80 条台词样例 | `synopsis`、`genre_hint`、`doc_tags`、`main_conflict` |
| **角色画像** | **批量**（默认 8 人/次） | `role_type_inferred`、`personality_tags`、`narrative_function` |
| **场次/关系/主题** | **按场**调用 | `scenes.*`、`relations(derived_by=llm)`、`themes` |
| **台词情感/受话人** | 关键台词 **批量** LLM（默认最多 100 条/剧） | `emotion_tag`、`target`、`speech_act`、`emotion_derived_by`、`target_derived_by` |
| **关系聚合** | 规则合并 + **LLM 校正** Top 48 边 + 再规则合并 | `relations_aggregated`、`llm_confidence`、`derived_by=llm_refined` |

关闭部分 LLM：`--no-llm-dialogue`、`--no-llm-relation-refine`。

### 阶段 4：分析层（`finalize_structured_package`）

- 稳定 ID：`char_id`、`scene_id`、`line_id`
- 别名：`entity_aliases.csv`（孔明→诸葛亮）
- 台词规则打底 + LLM 覆盖（r6）
- `parse_quality_score` / `parse_quality_label` / **`analysis_ready`**
- 衍生表：`theme_pairs`、`narrative_curve`、`network_metrics`
- 关系证据回链：`evidence_line_ids`（关联 `dialogues.line_id`）

### 阶段 5：全库汇总

```powershell
python mineru_batch_convert_structured_llm_final_v8.py --combine-only --output-dir opera_output
```

默认生成 `opera_output/all_docs.csv`、`all_roles.csv` … `all_structured.jsonl`。

---

## 三、核心文件与字段（r6 路径）

以下路径以 **r6 分层** 为准；r4 扁平时文件名相同，仅在剧目根目录。

### 3.1 `01_meta/documents.csv` — 文档总表

| 字段 | 含义 |
|------|------|
| `doc_id` | 全库唯一 ID |
| `collection_name` / `work_code` | 合集与作品编码 |
| `title`、`aliases`、`period_hint`、`genre_hint` | 剧名与元数据 |
| `synopsis`、`note_text`、`doc_tags` | 简介、注释、主题标签 |
| `parse_quality_score`、`parse_quality_label` | 0–1 与 high/medium/low |
| **`analysis_ready`** | **是否建议纳入全库统计（True/False）** |
| `scene_count` … `theme_pair_count` | 各表规模 |
| `structured_json` | 完整 JSON 路径 |

**分析入口**：先 `all_docs[analysis_ready==True]` 再 join 各子表。

### 3.2 `02_cast/roles.csv` — 角色（任务一）

| 字段 | 含义 |
|------|------|
| `role_type_raw` / `role_type_inferred` | 原文行当 vs 推断 |
| `personality_tags`、`behavior_tags`、`speech_style` | LLM/规则画像 |
| `narrative_function` | 主角/配角/功能性（r6 LLM） |
| `line_count`、`scene_coverage`、`centrality_hint` | 统计与网络提示 |

### 3.3 `03_script/scenes.csv` — 场次（任务四）

`summary`、`conflict_stage`、`tension_level`、`scene_function`、`narrative_turning_point`、`theme_labels`、各类 `*_density`。

### 3.4 `03_script/dialogues.csv` — 台词（全任务基础）

| 字段 | 含义 |
|------|------|
| `line_id`、`scene_id`、`line_no` | 回溯与关联 |
| `speaker`、`cue`、`text` | 谁说的、板式、正文 |
| `target`、`emotion_tag`、`speech_act` | 受话人、情绪、言语行为 |
| `emotion_derived_by`、`target_derived_by` | `rule` 或 `llm` |
| `entity_tags`、`action_tags`、`is_key_line` | 实体、动作、关键句 |
| `llm_confidence` | LLM 标注置信度（若有） |

### 3.5 `03_script/performances.csv` — 唱念做打

`perform_type`、`perform_subtype`、`action_tags`、`participants`。

### 3.6 `04_graph/relations.csv` — 分场关系（溯源）

`source`、`target`、`relation_type`、`evidence`、**`evidence_line_ids`**、`derived_by`（adjacency/mention/llm…）。

### 3.7 `04_graph/relations_aggregated.csv` — 全剧网络（任务二主表）

`weight`、`merged_relation_types`、`derived_by`（含 `llm_refined`）、`llm_confidence`。

### 3.8 `05_themes/*.csv` — 主题（任务三）

- `themes.csv`：`theme_label`、`theme_role_links`、`theme_stage`
- `themes_aggregated.csv`：全剧主题权重
- `theme_pairs.csv`：主题共现

### 3.9 `06_narrative/narrative_curve.csv` — 叙事曲线（任务四）

`tension_norm`、`is_climax`、`speech_density` 等，可直接画折线/面积图。

### 3.10 `04_graph/network_metrics.csv` — 网络指标（任务二）

`degree_centrality`、`strength_centrality`（**度中心性**；介数/社群需在分析阶段用 NetworkX 二次计算）。

### 3.11 `structured.json`

含 `metadata` 与全部子表数组；任务五前端可一次加载。

---

## 四、赛题五任务 ↔ 数据对照

### 任务一：行当与年代变化

| 需求 | 数据 | 操作 |
|------|------|------|
| 行当对比 | `all_roles` | `role_type_raw` vs `role_type_inferred` |
| 唱念做打 | `all_performances` + `dialogues` | 按角色统计 `perform_type` |
| 跨年代 | `all_docs.period_hint` + `all_roles` | 分组聚合 |
| 案例 | 《空城计》`02_cast/roles.csv` | |

### 任务二：关系网络与戏种比较

| 需求 | 数据 | 操作 |
|------|------|------|
| 全剧网络图 | `relations_aggregated` | 节点=角色，边=type，宽=weight |
| 中心人物 | `network_metrics` | 条形图 centrality |
| 分场子图 | `relations` + `scenes` | 按 `scene_index` 过滤 |
| 戏种对比 | join `all_docs.genre_hint` | 密度、类型占比 |

### 任务三：主题构成与组合

| 需求 | 数据 |
|------|------|
| 单剧构成 | `themes_aggregated` |
| 场次推进 | `themes` + `scenes` 热力图 |
| 共现模式 | `theme_pairs` 弦图/网络 |
| 主题—角色 | `themes.theme_role_links` |

### 任务四：叙事结构与节奏

| 需求 | 数据 |
|------|------|
| 张力曲线 | `narrative_curve` |
| 高潮 | `is_climax`、`narrative_turning_point` |
| 唱白节奏 | `performances` 按场统计 |

### 任务五：综合可视系统

| 模块 | 接口 |
|------|------|
| 剧目列表 | `all_docs.csv`（筛 `analysis_ready`） |
| 详情页 | `structured.json` 或分层 CSV |
| 联动 | 场次刷选 → 过滤 `relations` / `themes` → 点击边弹出 `dialogues`（`line_no`+`raw_line`） |

---

## 五、全库汇总文件（`opera_output/`）

| 文件 | 用途 |
|------|------|
| `all_docs.csv` | 全库索引、质量筛选 |
| `all_relations_aggregated.csv` | **任务二跨剧主表** |
| `all_narrative_curve.csv` | 任务四跨剧 |
| `all_dialogues.csv` | 语料（体积大） |
| `all_structured.jsonl` | 每剧一行 JSON，快速原型 |

---

## 六、数据质量与使用规范

### 6.1 质量过滤（必做）

```python
import pandas as pd
docs = pd.read_csv("opera_output/all_docs.csv")
good_ids = docs.loc[docs["analysis_ready"] == True, "doc_id"]
# 或：docs[docs["parse_quality_score"] >= 0.45]
```

**14 部 low**：多为 PDF 解析失败（1 场、0 关系），勿纳入全库结论。

### 6.2 关系表选用

| 场景 | 表 |
|------|-----|
| 全剧网络图 | `relations_aggregated` |
| 论文证据 | `relations` + `evidence` / `evidence_line_ids` |
| 中心性 | `network_metrics`（或自算介数） |

### 6.3 `derived_by` 含义

| 值 | 来源 |
|----|------|
| `llm` / `llm_refined` | 大模型 |
| `adjacency` / `mention` / `cooccurrence` | 启发式 |
| `keyword` | 主题关键词 |

### 6.4 目录与版本勿混用

- 分析用 **`opera_output`**，勿与 `opera_dataset_md` 混用  
- `parser_version` 为 r4 时无 `speech_act`、`01_meta/` 等 r6 字段  
- 升级 r6：见第九节

---

## 七、《空城计》示范（r4 磁盘数据参考）

| 问题 | 查表 | 参考 |
|------|------|------|
| 诸葛亮行当？ | `roles` | 老生 |
| 对话中心？ | `network_metrics` | 司马懿、诸葛亮 |
| 核心主题？ | `themes_aggregated` | 权谋、战争 |
| 高潮场次？ | `narrative_curve` | 首场 tension 高 |
| 汇报关系？ | `relations_aggregated` | 报子→诸葛亮 |

当前质量分约 **0.963**，143 条台词，81/43 条关系，属 **analysis_ready** 优质样本。

---

## 八、是否满足赛题全部要求？

| 维度 | 满足度 | 说明 |
|------|--------|------|
| 数据模型（文档+5表+图谱） | **约 90%** | 表齐全，路径 r6 已规范 |
| 五任务可直接开做 | **是（36/50 部）** | 需 `analysis_ready` 过滤 |
| 行当/唱念做打 | **高** | roles + performances |
| 关系网络 | **中高** | 聚合边+度中心性；介数/社群待分析端补 |
| 主题与共现 | **中高** | theme_pairs + theme_role_links |
| 叙事曲线 | **中高** | narrative_curve 可直接绘图 |
| 综合系统 | **是** | structured.json + all_* |
| PDF 低质量剧 | **部分失败** | 14 部需换源或手工修 md |

**尚未自动化、建议在分析/notebook 中补充**：介数中心性、社群划分、跨剧时空统计检验、多版本剧本 diff、情感细粒度分类器微调。

---

## 九、大语言模型利用评估（r6 设计）

| 环节 | 是否用 LLM | 策略 |
|------|------------|------|
| 文档元数据 | ✅ | 前言 + 台词样例 + 集合上下文 |
| 角色画像 | ✅ | 批量，降 API 次数 |
| 场次/关系/主题 | ✅ | 按场，控制 `max_scene_lines` |
| 台词情感/受话人 | ✅（r6） | 关键台词批量，规则兜底 |
| 关系聚合语义 | ✅（r6） | Top 边 LLM 校正 + 规则合并 |

**利用率**：由 r4 的「场次级」提升到 r6 的 **全链路增强**；仍非逐句全量（成本与 `max_dialogue_llm_lines=100` 限制）。  
**磁盘现状**：50 部仍为 r4 结果；要获得 r6 字段须 **重跑** 或至少 **repack + 单剧重解析**。

---

## 十、批量处理《戏考》01000000（448 部）

当前进度（`--status-only`）：**已完成 50 部（r4）**，**待处理 398 部**；升级 r6 需对已完成剧目加 `--no-skip-existing` 重跑。

### 10.0 推荐流程

```powershell
cd D:\University_studies\Junior\data_visualization\experiment\final_self
$env:MINERU_TOKEN = "你的MinerU密钥"
$env:DEEPSEEK_API_KEY = "你的DeepSeek密钥"

# 1) 查看进度（免费）
python mineru_batch_convert_structured_llm_final_v8.py `
  --status-only --input-dir opera_dataset\01000000 --output-dir opera_output

# 2) 冒烟 2 部（r6+LLM）
python mineru_batch_convert_structured_llm_final_v8.py `
  --input-dir opera_dataset\01000000 --output-dir opera_output `
  --llm-enabled --limit 2 --no-skip-existing

# 3) 全量 448 部（约 9 个 MinerU 批次，耗时长）
python mineru_batch_convert_structured_llm_final_v8.py `
  --input-dir opera_dataset\01000000 --output-dir opera_output `
  --llm-enabled --chunk-size 50 --no-skip-existing `
  --manifest opera_output\mineru_manifest_01000000.csv

# 4) 中断后仅汇总
python mineru_batch_convert_structured_llm_final_v8.py `
  --combine-only --output-dir opera_output --collection-prefix 01000000

# 5) 质量检验 + 示例图
python analysis_starter.py --verify --collection 01000000
python analysis_starter.py --title 空城计 --play-dir opera_output\01000000\01001001_空城计
```

也可直接运行辅助脚本：`.\run_opera_01000000.ps1`（按注释取消对应步骤）。

批处理结束后会自动写入 `opera_output/all_*.csv`（`--no-auto-combine` 可关闭）。

---

### 10.1 仅重组目录（不耗 MinerU/LLM，现有 50 部）

```powershell
python mineru_batch_convert_structured_llm_final_v8.py --repack-only --output-dir opera_output
python mineru_batch_convert_structured_llm_final_v8.py --combine-only --output-dir opera_output
```

### 10.2 全量升级 r6 + LLM（推荐对单剧试跑后全库）

```powershell
python mineru_batch_convert_structured_llm_final_v8.py `
  --input-dir opera_dataset `
  --output-dir opera_output `
  --llm-enabled `
  --no-skip-existing `
  --clean-md
```

汇总（默认写入 `opera_output/all_*.csv`）：

```powershell
python mineru_batch_convert_structured_llm_final_v8.py --combine-only --output-dir opera_output
```

### 10.3 常用 LLM 参数

| 参数 | 默认 | 含义 |
|------|------|------|
| `--llm-max-dialogue-lines` | 100 | 每剧 LLM 标注台词上限 |
| `--llm-dialogue-batch-size` | 18 | 台词批大小 |
| `--no-llm-dialogue` | - | 关闭台词 LLM |
| `--no-llm-relation-refine` | - | 关闭关系 LLM 校正 |

---

## 十一、分析脚手架 `analysis_starter.py`

一键从 `all_*.csv` 生成赛题示例图（PNG + JSON 摘要）：

```powershell
pip install pandas matplotlib networkx

python analysis_starter.py --data-dir opera_output --title 空城计 `
  --play-dir opera_output/01000000/01001001_空城计
```

| 输出文件 | 对应任务 |
|---------|---------|
| `analysis_figures/00_corpus_overview.png` | 全库质量/戏种/analysis_ready |
| `analysis_figures/<剧名>/01_relation_network.png` | 任务二 人物关系网络 |
| `analysis_figures/<剧名>/02_narrative_curve.png` | 任务四 叙事张力曲线 |
| `analysis_figures/<剧名>/03_themes.png` | 任务三 主题条形图 |
| `analysis_figures/<剧名>/04_role_centrality.png` | 任务二 度中心性 |
| `analysis_figures/<剧名>/play_summary.json` | 前端/报告用的剧目摘要 |

常用参数：`--doc-id`、`--ready-only`、`--min-weight 3`、`--skip-corpus`。

---

## 十二、推荐分析工作流

1. `all_docs.csv` → 质量分布、`analysis_ready` 筛选  
2. 任务二：`all_relations_aggregated` + `all_network_metrics`  
3. 任务四：`all_narrative_curve` 多剧对比  
4. 案例：《空城计》`structured.json` 五视图联动  
5. 论文证据：`relations.evidence_line_ids` → `dialogues.text`  
6. 快速出图：运行 `analysis_starter.py`  

---

## 十三、小结

| 问题 | 答案 |
|------|------|
| 是否满足赛题全部数据处理要求？ | **基本满足（约 85–90%）**，低质量剧与部分图算法需分析端补充 |
| 还能优化吗？ | 能：重跑 r6、低质量 PDF 修复、分析端介数/社群、可选全句 LLM |
| LLM 是否充分利用？ | **r6 设计已全链路**；**磁盘 r4 尚未体现台词/关系 LLM** |
| 能否直接用于后续分析？ | **能**：`all_*` + `analysis_ready`；单剧用 `structured.json` 或分层 CSV |

---

*文档与 `mineru_batch_convert_structured_llm_final_v8.py`（**2026-05-16-r6**）同步；字段以脚本内 `write_play_package` / `write_combined_exports` 为准。*
